# pipeline-sandbox
A sandbox GitHub repository used to demonstrate pipeline portability — the agent pipeline (Jira → orchestrator → Cloud Run worker → PR) pointed at a completely separate client setup from the main Media Sage project.
